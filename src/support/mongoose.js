export default {
  // useNewUrlParser: true,
  // autoIndex: false,
  // connectTimeoutMS: process.env.MONGO_TIMEOUT,
  // reconnectTries: Number.MAX_VALUE, // Never stop trying to reconnect
  // reconnectInterval: process.env.MONGO_RECONNECT_INTERVAL, // Reconnect every 500ms
  // poolSize: process.env.MONGO_POOL_SIZE, // Maintain up to 10 socket connections
  // keepAlive: (process.env.MONGO_KEEPALIVE !== undefined),
  // keepAliveInitialDelay: process.env.MONGO_KEEPALIVE,
};
